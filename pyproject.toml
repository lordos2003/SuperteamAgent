[project]
name = "superteam-agent"
version = "1.0.0"
description = "Superteam Earn hybrid search: Agent API + public website + public card verification"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27,<1.0",
    "python-dotenv>=1.0,<2.0",
    "openpyxl>=3.1,<4.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
